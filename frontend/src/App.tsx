/**
 * Milestone 1 placeholder.
 *
 * The real UI is built in milestones 4-7:
 *   - Today's Study Plan  (ranked recommendation cards)
 *   - Dashboard           (mastery heatmap by domain)
 *   - Log Attempt         (form that updates mastery live)
 */
export default function App() {
  return (
    <main className="mx-auto max-w-2xl px-6 py-16">
      <h1 className="text-2xl font-semibold text-slate-900">SAT StudyPath</h1>
      <p className="mt-3 text-slate-600">
        Personalized SAT study-path generator. Backend, data model, and seed data are in
        place; the UI is wired up in a later milestone.
      </p>
    </main>
  )
}
